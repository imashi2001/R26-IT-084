/** Re-export shared dashboard UI tokens (backward compatibility). */
export {
  inputClass,
  selectClass,
  labelClass,
  btnPrimary,
  btnSecondary,
  btnGhost,
  fillBadgeClass,
  statusBadgeClass,
  riskBadgeClass,
  bannerTone,
  summaryTone,
} from "../dashboard/dashboardUi";

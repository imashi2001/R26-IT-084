# Litter Severity Detection — YOLO11 + LSI pipeline
# Install: pip install -r requirements.txt
# Colab:   pip install -q -r requirements.txt

ultralytics>=8.3.0
opencv-python>=4.8.0
matplotlib>=3.7.0
numpy>=1.24.0
PyYAML>=6.0
torch>=2.0.0
torchvision>=0.15.0

# Optional: pull dataset directly inside Colab
# roboflow>=1.1.0

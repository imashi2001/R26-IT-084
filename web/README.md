# Deprecated — use `frontend/` instead

This folder is a legacy copy of an older React app structure. It is **not** deployed
and may be missing recent features (collection routes, virtual bins, dark dashboard theme).

**Canonical app:** [`../frontend/`](../frontend/)

Do not edit files here unless you are intentionally syncing from `frontend/`.
